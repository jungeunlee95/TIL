[[[ 리턴함수 ]]]

호출자가 피호출자로부터 값을 받고싶을 때
 return 함수를 사용함


호출자가 피호출자를 호출했는데
피호출자가 return; 을 만나면 다시 호출자 메소드로 강제복귀


근데 return할때 피호출자가 호출자에게 값을 전달하고싶으면,
return (전달하고싶은것) ;  하면 됨  - > 이 때 void자리에 전달할 값의 자료형 적기


호출자가 return값을 받으려면 대입을 꼭 써야함
int sum = calc(10,20)


비호출자 -> 호출자로
리턴할수있는 값은 무조건 한 개만 가능함 
여러값을 넘기고 싶을땐?? 
                                     => char[] arr = {'A','B'} 배열을 만든 뒤
                                          return arr; 으로 넘기고,

호출자공간에서는 = char[] ar = f(); 형태로 받아야함
                        =자료형[] 변수이름 = 위치.메소드이름();
















































